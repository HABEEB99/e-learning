const Header = () => {
  return <div>Header</div>;
};

export default Header;

// GET_SOCKET_URL: (trail_id: string, auth_token: string) => {
//     return `https://${process.env.NEXT_PUBLIC_BACKEND_SERVICE}/socket/message-feed/${trail_id}/ws?auth_token=${auth_token}`;
//   },
